export enum PostTypes {
  post = 'post',
  page = 'page',
  story = 'story',
  series = 'series',
}

export enum Status {
  draft = 'draft',
  scheduled = 'scheduled',
  review = 'review',
  published = 'published',
}
